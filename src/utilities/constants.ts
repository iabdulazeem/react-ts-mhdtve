/**
 *
 * This will include all the hardcoded values, colors and bussiness rules etc
 * Colors could be moved to a separate file in case there's a theme implementation is required
 *
 */

export const CANVAS_BACKGROUND = '#EDEDED';
export const WINDOW_BORDER_COLOR = '#FEDD00';
